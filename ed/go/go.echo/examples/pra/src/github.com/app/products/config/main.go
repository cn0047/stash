// All project configs must be specified here.

package config

// ApplicationRoot - application root directory.
const ApplicationRoot = "/app"

// ProductsArchive - default products archive name.
const ProductsArchive = "products.zip"

// DataStorageDirectory - default data storage directory name.
const DataStorageDirectory = "data"

// ProductsStorageDirectory - default products storage directory name.
const ProductsStorageDirectory = "products"
